import 'package:flutter/material.dart';

class Widget2Screen extends StatelessWidget {
  const Widget2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}